apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "py.fullname" . }}-test-connection"
  labels:
    {{- include "py.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "py.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
